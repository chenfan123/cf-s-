/**
 * 目标：实现返回防抖值的 Hook。
 * 要求：value 或 delay 改变时重设计时器，并正确清理旧计时器。
 * 示例：输入连续变化，停止 500ms 后 useDebounce(value, 500) 才返回新值。
 */
export function useDebounce<T>(value: T, _delay = 500): T {
  // TODO: 在这里完成实现
  return value
}

