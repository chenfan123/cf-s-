/**
 * 目标：实现保存“上一次渲染值”的 Hook。
 * 要求：首次渲染返回 undefined，保存旧值不能引发额外渲染。
 * 示例：value 从 10 变为 20 后，usePrevious(value) 返回 10。
 */
export function usePrevious<T>(_value: T): T | undefined {
  // TODO: 在这里完成实现
  return undefined
}

